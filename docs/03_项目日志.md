# VibeHub 项目日志

版本：v1.0  
更新日期：2026-04-12

## 日志模板

| 日期 | 阶段 | 目标 | 关键行动 | 输出物 | 风险/阻塞 | 决策 | 下一步 |
|---|---|---|---|---|---|---|---|
| YYYY-MM-DD | PX | ... | ... | ... | ... | ... | ... |

## 日志记录

| 日期 | 阶段 | 目标 | 关键行动 | 输出物 | 风险/阻塞 | 决策 | 下一步 |
|---|---|---|---|---|---|---|---|
| 2026-04-12 | P1 | 落地全栈MVP骨架 | 手工搭建 Next.js 工程；实现 `/api/v1` 与 MCP v1；补齐文档资产 | `web/` 代码、`docs` 三文档、部署模板 | npm 官方脚手架超时 | 采用手工脚手架方案，优先保证可交付 | 安装依赖并联调 API 与页面，切换真实 PostgreSQL |
| 2026-04-12 | P1 | 完成 P1 质量门禁闭环 | 修复鉴权漏洞与开放重定向；统一 DTO 序列化；补齐构建链路与 CI；新增关键单测 | 安全修复代码、`p1-gate` CI、测试通过、构建通过 | 依赖网络不稳定导致安装中断 | 切换镜像源完成依赖安装并跑通 `lint/test/build` | 准备进入 P2 的后台 RBAC 与审核工作流设计 |

## 2026-04-12 P2-3 Log

- Goal: ship topic collections, discussion/project leaderboards, and collaboration intent conversion metrics as the next P2 executable unit.
- Actions: added curated topic config and discovery APIs; leaderboard queries (SQL for Postgres, in-memory for mock); public funnel metrics API; UI routes `/collections`, `/collections/[slug]`, `/leaderboards`; extended admin overview with funnel cards; navigation and discussion list anchors; Vitest coverage for mock path.
- Outputs: new API routes under `/api/v1/collection-topics`, `/api/v1/leaderboards/*`, `/api/v1/metrics/collaboration-intent-funnel`; repository helpers; updated docs and project log.
- Risk: raw SQL for leaderboards must stay aligned with Prisma table/column names when schema evolves.
- Decision: keep topics as config-driven tag slices (no new DB table) for faster delivery; leaderboards are global rankings (not time-windowed) until analytics tables exist.
- Next: P2-4 — investor read-only view (see kickoff below).

## 2026-04-12 P2-3 merge & P2-4 kickoff

- Decision: merge [PR #1](https://github.com/KBREX684/vibehub/pull/1) (P2-3) into `main`, then continue on branch `cursor/p2-4-investor-view-community-40ca`.
- P2-4 focus (**社区运营向**): ship an **investor-style discovery view** that doubles as an **ops-friendly project radar** — curated filters (tags / tech stack / project status), readable list + detail deep links, optional “staff picks” or highlight flags later without blocking MVP.
- Concrete next build steps (order): (1) `GET /api/v1/projects` query extensions + stable DTO for list cards; (2) public page e.g. `/discover` or `/investors` with filter UI and shareable query string; (3) empty-state and copy aimed at moderators/BD, not only capital; (4) tests on repository filters (mock + Prisma).
- Deferred in P2-4: time-windowed weekly leaderboard tables, full challenge campaign pages (can follow as P2-5).

## 2026-04-12 P2-4 Log (investor / ops discovery)

- Goal: deliver a read-only **project radar** for community ops and external discovery (aligned with plan “投资者筛选视图” read-only slice).
- Actions: extended `listProjects` with `tech` and `status`; added `getProjectFilterFacets` and `GET /api/v1/projects/facets`; `GET /api/v1/projects` parses `tech`/`status`; MCP `search_projects` accepts the same optional filters; new `/discover` page with filters, pagination links preserving query string, ops-oriented empty state; nav link **Discover**.
- Outputs: `web/src/app/discover/page.tsx`, API routes under `projects/`; `tests/project-list-filters.test.ts`.
- Risk: Postgres `tech` filter uses exact string match on `techStack` array — seed data casing must match UI facet values.
- Decision: keep list DTO as existing `Project` type; no new “investor” role gate on this page (public read-only).
- Next: optional **staff picks** / pinned projects (config or admin flag), MCP `search_projects` parity for new filters, or P2-5 weekly materialized rankings.

## 2026-04-12 P2-5 Log (weekly leaderboard materialization)

- Goal: ship **time-windowed weekly rankings** with optional **materialized snapshots** so public pages stay stable after the week closes.
- Actions: added Prisma snapshot tables; `getWeeklyDiscussionLeaderboard` / `getWeeklyProjectCollaborationLeaderboard`; `getWeeklyLeaderboardPublicPayload` (prefers materialized rows); admin materialize endpoint + audit log; public weekly leaderboard APIs; extended `/leaderboards` with week navigation; admin dashboard materialize form; Vitest coverage for UTC week parsing and mock materialization path.
- Outputs: migration under `web/prisma/migrations/`, new API routes, repository helpers, UI updates, `tests/p2-5-weekly-leaderboards.test.ts`.
- Risk: week boundary is **UTC Monday** — product copy documents this; cross-timezone “local week” is out of scope for this slice.
- Decision: when no snapshot exists, public APIs fall back to **live** weekly counts so empty databases still behave sensibly.
- Next: staff picks / featured projects, challenge campaign pages, or MCP parity for discover filters.

## 2026-04-12 P3-1…P3-6 全量审计与收口

- Goal: 对 P3-1（团队）至 P3-6（任务排序）做一次可复核的代码审计，修复审计中发现的问题，并将主线合入 `main`、同步文档。
- Actions: 交叉核对 Prisma 迁移链、团队相关 API routes、`repository` 中团队/入队/项目绑定/任务/里程碑逻辑与现有 Vitest；合并 `cursor/p3-6-team-task-sort-dfaf` 至 `main`；落地 **BUG-P3-1-002**：`createTeam` 在 slug 冲突时生成候选 slug 始终 **≤48** 字符（mock 与 DB 路径）；Debug 表登记；实现计划图 v1.9 审计段。
- Outputs: `web/src/lib/repository.ts`（`buildTeamSlugCandidate`）、`tests/team-repository.test.ts` 回归用例、`docs/02_Debug表.md`、`docs/01_实现计划图.md`、本条日志。
- Risk: 仍为 MVP 权限模型（如任务任意成员可删）；后续切片再收紧 RBAC。
- Decision: 审计中确认的 **slug 超长** 类缺陷在合入主线前必须修复，避免生产写入违反 schema。
- Next: P3-7 **里程碑与任务关联**（已实现）；团队通知；任务板按状态分列（已实现，见 P3 扫尾日志）。

## 2026-04-13 P3 主线扫尾（分列看板 + 列内重排语义）

- Goal: 在不动迁移的前提下，把团队任务板做成 **todo / doing / done 三列**，并让 **上移/下移** 与列语义一致（仅同列内调整 `sortOrder`）。
- Actions: `TeamTasksPanel` 分列布局 + 文案；`globals.css` 看板样式；`reorderTeamTask` mock/Prisma 按 `status` 过滤后再交换；`web/README` 补充 reorder 语义；Vitest 重排用例改为同列两条临时任务。
- Outputs: 无新迁移；行为与 UI 对齐路线图「看板按状态分列」。
- Next: **团队通知 / 站内提醒**（P3 与「订阅」方向的衔接），或进入 P4 准备（若产品冻结 P3）。

## 2026-04-13 P3-7 Log（任务 ↔ 里程碑关联）

- Goal: **TeamTask** 可选归属本团队 **TeamMilestone**，删除里程碑时任务自动解除关联（DB 由 FK 处理，mock 显式清空）。
- Actions: Prisma `milestoneId` + 迁移；`listTeamTasks` / `createTeamTask` / `updateTeamTask` / `deleteTeamMilestone`；REST `milestoneId`；团队页任务板下拉；seed 顺序调整；Vitest（`team-task-repository`、`team-milestone-repository`）。
- Outputs: `web/prisma/migrations/20260413000000_p3_7_team_task_milestone_link`。
- Next: 团队通知 / 站内提醒（看板分列已在 P3 扫尾完成）。

## 2026-04-12 P3-6 Log（任务排序）

- Goal: **TeamTask** 在团队内可手动排序（路线图「任务排序」最小切片）。
- Actions: Prisma `sortOrder` + 迁移；`listTeamTasks` / `createTeamTask` / `updateTeamTask` 扩展；新增 `reorderTeamTask`；`POST .../tasks/:taskId/reorder`；团队页任务板上移/下移；seed 显式 sortOrder；Vitest 覆盖重排。
- Outputs: `web/prisma/migrations/20260412230000_p3_6_team_task_sort_order`，`tests/team-task-repository.test.ts` 增补。
- Decision: `web/package.json` 增加 `prebuild` 执行 `prisma generate`，避免 schema 变更后 `next build` 仍用旧 client 类型导致 seed 等编译失败。
- Next: P3-7 **里程碑与任务关联**（已实现）或团队通知。

## 2026-04-12 P3-5 Log（团队里程碑）

- Goal: **TeamMilestone** 时间线（目标日期 + 完成勾选 + 排序字段），对齐路线图「里程碑」最小切片。
- Actions: Prisma 模型与迁移；`listTeamMilestones` / `createTeamMilestone` / `updateTeamMilestone` / `deleteTeamMilestone`；REST；团队页 `TeamMilestonesPanel`；mock 两条里程碑；seed；Vitest；修复 mock `TeamMembership` 的 `id` 生成（避免与 `removeTeamMember` 的 `findIndex` 误删，连带稳定 P3-2 单测）。
- Outputs: `web/prisma/migrations/20260412220000_p3_5_team_milestones`，`tests/team-milestone-repository.test.ts`。
- Next: P3-6 **任务排序**（已实现）；或 **里程碑与任务关联**；或通知/提醒。

## 2026-04-12 P3-4 Log（团队任务板）

- Goal: **TeamTask** 轻量任务（状态 + 指派），对齐路线图「协作空间」最小切片（非里程碑/订阅）。
- Actions: Prisma `TeamTask` + migration; `listTeamTasks` / `createTeamTask` / `updateTeamTask` / `deleteTeamTask` + 成员校验与指派校验；REST；团队详情页 `TeamTasksPanel`；mock 种子任务；seed 两条任务；Vitest。
- Outputs: `web/prisma/migrations/20260412200000_p3_4_team_tasks`，`tests/team-task-repository.test.ts`。
- Risk: 任意成员可删任意任务（MVP）；后续可加「仅创建者/队长」策略。
- Next: P3-5 **里程碑**（已实现）；后续可做任务排序或里程碑-任务关联。

## 2026-04-12 P3-3 Log（项目归属团队）

- Goal: **link projects to teams** for the roadmap “Team 空间 + 项目”最小闭环（非任务板）。
- Actions: added `teamId` on `Project`; extended `listProjects` / DTOs; `updateProjectTeamLink` + `listTeamsForUser` / `getCreatorProfileById`; APIs `PATCH /api/v1/projects/:slug`, `GET /api/v1/me/teams`; discover team dropdown; team page project list; creator-only team picker on project detail; seed links vibehub→vibehub-core; Vitest.
- Outputs: migration `20260412180000_p3_3_project_team_link`, new client form + routes.
- Risk: **one team per project** (single FK); multi-team would need a join table later.
- Decision: only **project creator** may set `teamSlug`; actor must already be a **team member** (prevents arbitrary tagging).
- Next: P3-4 **TeamTask** board（已实现）；后续可做 **任务排序** 或里程碑-任务关联（P3-6）。

## 2026-04-12 P3-2 Log（团队入队申请 + 队长审批）

- Goal: replace **open join** with **apply + owner review**, matching roadmap “成员申请与审核”的最小切片。
- Actions: added `TeamJoinRequest` model and migration; `requestTeamJoin` / `reviewTeamJoinRequest`; updated `getTeamBySlug(slug, viewerUserId)`; join API + review API; team detail UI (apply form, pending banner, owner queue); email-invite clears pending requests; Vitest coverage.
- Outputs: migration `20260412160000_p3_2_team_join_requests`, new API route under `join-requests/.../review`, repository + UI updates.
- Risk: at most one join-request **row** per user per team (unique); rejected requests are **reopened** to `pending` on re-apply instead of inserting a second row.
- Decision: owner direct-invite remains **instant member** for ops efficiency.
- Next: P3-3 — team-linked **projects** or lightweight **task** rows.

## 2026-04-12 P3-1 Log（Team MVP）

- Goal: open **P3** with the smallest **Team** slice aligned to the roadmap (team home + membership).
- Actions: Prisma `Team` / `TeamMembership` + migration; repository + REST under `/api/v1/teams/*`; pages `/teams` and `/teams/[slug]` with create/join/invite/remove flows; mock data + Vitest; seed `vibehub-core`.
- Outputs: migration `20260412140000_p3_1_teams`, new routes and components, `tests/team-repository.test.ts`.
- Risk: membership is **open join** (any logged-in user) plus **owner email invite** — no application queue yet; refine in P3-2 if needed.
- Decision: single **owner** role matches “one-person company + agents” default; later add admin/co-owner if product requires.
- Next: P3-2 candidates — team-linked projects, join **requests** + approve, or lightweight task board.

## 2026-04-12 P2 全量收口（审计结论）

- **范围**：以本仓库可执行切片 **P2-1 … P2-5** 为「P2 完整部分」验收边界（管理后台与互动增强主线）；对照 `VibeHub_项目计划书_v3.0.md` §4.2 的缺口单独列出延期项，不阻塞本阶段代码收口。
- **质量门禁**：`web` 目录下 `npm run check`（lint + vitest + `next build`）在 P2-5 分支末次提交通过；API 清单与 `web/README.md` 已对齐至 `src/app/api/v1/**/route.ts`。
- **数据库**：P2 引入的 Prisma 变更需在生产环境执行 migrate（含 P2-5 `WeeklyLeaderboardSnapshot` / `WeeklyLeaderboardRow`）；`USE_MOCK_DATA=false` 时需配置 `DATABASE_URL` 与 `SESSION_SECRET`。CI（`p1-gate`）已对 `main`/PR 在临时 Postgres 上执行 `prisma migrate deploy`，避免损坏的迁移进入主干。
- **合并路径**：`origin/main` 当前 tip 含至 **P2-3**；P2-4 与 P2-5 的代码在分支 `cursor/p2-5-weekly-leaderboard-snapshots-dfaf` 上已连续提交（含 discover 与周榜物化）。将该分支合并入 `main` 并执行 migrate 后，即完成本仓库 **P2-1…P2-5** 主线交付。
- **计划书未纳入本阶段**：挑战赛活动页、创作者成长面板、独立「精华」机制 — 记入 `docs/01_实现计划图.md` 对照表，建议进入 P3 或拆独立迭代。

## 2026-04-12 P2-2 Log

- Goal: deliver collaboration intent entry and moderation closure as the next executable unit in P2.
- Actions: implemented collaboration intent data model, APIs, project detail form, admin collaboration queue, and review actions.
- Outputs: new endpoints, new admin route (`/admin/collaboration`), updated project detail page, new tests.
- Risk: DB schema changed (requires migration in real DB environment).
- Decision: keep behavior backward compatible under `/api/v1`, add new resources only.
- Next: proceed to P2-3 (`专题/榜单/协作意向转化指标`) and strengthen audit analytics.
