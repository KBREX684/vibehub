# VibeHub v11.0 — 前端任务计划书（GLM 执行）

> 配合 RFC：`docs/v11.0-final-chapter-rfc.md`
> 适用对象：GLM（前端工程师角色）
> 使用方式：把每个 P 阶段对应的 prompt 整段复制给 GLM，让它先用 plan 模式拆解，再执行。
> 仓库结构假定：Next.js + Tailwind + 自带 ui/ 组件库 + i18n（zh/en），代码在 `web/`。

---

## 0. 你的角色（GLM 必读）

```text
你是 VibeHub v11.0 的前端工程师。后端工程师由 GPT 担任，你不动后端。

你只负责修改：
  - web/src/app/**/page.tsx          (页面)
  - web/src/app/**/layout.tsx        (布局)
  - web/src/components/**            (组件)
  - web/src/locales/**               (i18n 文案)
  - web/src/lib/data/mock-*          (前端 fixture，让 USE_MOCK_DATA=true 也能跑)
  - web/src/app/globals.css          (全局样式 token，仅在 RFC 允许时改)
  - web/tests/e2e/**                 (Playwright UI E2E)

你绝不动：
  - web/prisma/**                    (数据库)
  - web/src/app/api/**               (API route)
  - web/src/lib/repositories/**      (后端 repository)
  - web/src/lib/openapi-spec*.ts     (OpenAPI doc)
  - web/src/lib/ledger/**            (后端账本逻辑)
  - web/src/lib/aigc/**              (后端水印逻辑)
  - web/src/lib/legal-anchor/**      (后端司法锚定)
  - web/src/lib/jobs/**              (后台任务)
  - web/scripts/**                   (后端脚本)
  - web/tests/*.test.ts              (后端单测，不含 e2e)
  - packages/vibehub-verify/**       (后端 CLI 包)

共享文件需要改时（如 web/src/lib/types.ts、web/src/lib/generated/api-types.ts、
web/src/middleware.ts、package.json），你只生成 patch 内容并在 PR 描述里
明确写"@PM 需手动 merge：[文件路径]，原因：[...]"，由 PM 决定是否合入。

你必须遵守的设计 DNA（来自 DESIGN.md + globals.css）：
  - 暗色优先，画布 #09090B，主面 #101014
  - 字体：Inter（UI）+ Geist Mono（数字 / 哈希 / 时间 / 状态）
  - 主色：Apple Blue #0071e3（仅一个 CTA 主色）
  - 强调色：Violet #a78bfa、Cyan #22d3ee（按语义使用）
  - 风格参照：Linear（密度）+ Vercel（克制）+ Raycast（命令）+ Supabase（表）
  - 视觉新增元素只允许一个：印戳 Stamp（虚线方框 + Geist Mono 短哈希）
  - 禁止：light mode、glow gradient、3D、玻璃拟态、政务红章、聊天气泡

每个阶段：
  1. 收到任务后先用 plan 模式列出执行步骤、影响文件、新增组件、验收命令
  2. plan 经 PM 确认后开始执行
  3. 严格按"任务粒度 = 一个 commit"
  4. 每个 API 调用都必须有对应的 mock fixture，能在 USE_MOCK_DATA=true 下跑通
     不联调真实后端
  5. 提交前必须本地通过：
       npm run lint --workspace=web
       npm run typecheck --workspace=web
       USE_MOCK_DATA=true npm run dev    # 手动 smoke
       npm run e2e --workspace=web -- v11-<阶段>
  6. PR 标题以阶段 ID 开头，如：[v11/P3] /studio shell + 6 view tabs

参考文档：
  - 产品定义：docs/v11.0-final-chapter-rfc.md
  - 你这份：docs/v11.0-frontend-tasks.md
  - 设计 token：DESIGN.md + web/src/app/globals.css
  - 当前页面：web/src/app/
  - 当前组件：web/src/components/

如有任何"是不是该问 GPT"的疑虑：不要联系后端，直接按本文件契约 + fixture 执行。
后端会基于本文件里的 TS 类型与 API 路径自己实现。
```

---

## 1. 阶段总览

| 阶段 | 主题 | 关键产出 | 集成 Gate |
| --- | --- | --- | --- |
| P0 | 减法收口 | 顶栏 3 项；删 6 个旧路由；Pricing 两档；文案清洗 | 顶栏视觉对齐 RFC §3；旧路由 404 |
| P1 | （前端无任务，等 P2） | — | — |
| P2 | AIGC Toggle + LedgerStampBadge | 2 个新组件 + mock fixture | Storybook / 手动 smoke |
| P3 | Studio Shell + 6 view | StudioShell（重命名）+ /studio 整合 | E2E：6 个 view 切换 |
| P4 | Ledger 页 + Timeline | /ledger 上线 + 导出对话框 | E2E：列表 + 过滤 + 导出 |
| P5 | Trust Card | /u/[slug] 重做 + 6 指标网格 + 公开 PDF 入口 | E2E：未登录访问公开 |
| P6 | LegalAnchorCard | 锚定按钮 + UpgradePrompt | E2E：free 用户提示升级 |
| P7 | 品牌叙事 | Home / Pricing / Footer / 印戳全站 | 旧词全清零 |
| P8 | PMF 埋点接入 + admin dashboard | 全站埋点 + dashboard UI | 触发埋点 → 可见数字 |

---

## 2. P0 — 减法收口

### 2.1 给 GLM 的完整 prompt

```text
任务 ID：[v11/P0][FE] 顶栏减为 3 项 + 删除旧路由 + Pricing 两档 + 文案清洗

目标：
  按 RFC §5.1 把 v10 的多余路由从前端拿掉，让访问者只看到 3 个一级入口。
  后端这一阶段由 GPT 把对应 API 改 410，你不需要管。

执行步骤（先 plan，再做）：

  1. 重构 web/src/components/site-nav.tsx
     - NAV_LINKS 常量改为：
         { href: "/", key: "nav.home" }
         { href: "/studio", key: "nav.studio" }
         { href: "/pricing", key: "nav.pricing" }
     - 已登录用户：右上角保留 [+ 新建] [升级] [🔔] [头像]
       创建菜单只保留 1 项："新建工作记录"，链接 /studio
     - 删除：发现 / 工作台 / 项目库 / 创建团队 / 创建意向 等链接
     - 移动端菜单同步收口

  2. 删除以下页面目录（不留 stub，让 Next.js 自然 404）：
     - web/src/app/work/team/
     - web/src/app/work/intents/
     - web/src/app/work/library/
     - web/src/app/work/agent-tasks/
     - web/src/app/work/create-team/
     - web/src/app/work/notifications/
     - web/src/app/discover/
     - web/src/app/leaderboards/
     - web/src/app/collections/

  3. /work/personal 暂保留（P3 才会改名为 /studio）；
     /work/page.tsx 改为 redirect 到 /studio：
       import { redirect } from 'next/navigation';
       export default function Page() { redirect('/studio'); }

  4. 创建占位页 web/src/app/studio/page.tsx：
     一个简单页面，正中显示"Studio 即将上线（P3 完成后启用）"
     使用现有 EmptyState 组件，不要自己写样式

  5. Pricing 页改两档（web/src/app/pricing/page.tsx + web/src/components/pricing-cards.tsx）：
     - 删除 Team / Enterprise 卡
     - Free：¥0 · 1 GB · 100 ledger/月 · 基础 AIGC 标识 · 公开 Trust Card 基础版
     - Pro：¥29/月 或 ¥288/年 · 10 GB · 无限 ledger · 完整 AIGC 标识 · 至信链锚定 · 月度合规报告 · Trust Card 高级版
     - 文案以中英两版同步进 zh.json / en.json

  6. 全站文案清洗（仅改 value，不改 key，避免破坏组件引用）：
     - web/src/locales/zh.json 与 en.json
     - 替换关键词（仅 value 中）：
         "协作网络"  → "AI 留痕本"
         "协作中枢"  → "操作公证账本"
         "团队工作空间" → "Studio"
         "工作台"    → "Studio"
         "协作意向"  → 全部相关条目删除（找不到对应组件就别动）
     - 验证：grep -r "协作网络" web/src/locales/ 应为空

  7. E2E：web/tests/e2e/v11-p0-removed.spec.ts
     - 访问 /work/team/anything → 期望 404
     - 访问 /work/intents → 404
     - 访问 /discover → 404
     - 访问 /work → 期望 redirect 到 /studio
     - 访问 / 顶栏 nav 只有 3 个 link

不要做：
  - 不要去改后端 API（GPT 负责返回 410）
  - 不要重命名 web/src/app/work/personal/（P3 才做）
  - 不要新建任何业务组件（P2 才开始）

验收（你必须本地跑通后才能提 commit）：
  - npm run lint --workspace=web
  - npm run typecheck --workspace=web
  - USE_MOCK_DATA=true npm run dev → 手动 smoke 顶栏 + Pricing
  - npm run e2e --workspace=web -- v11-p0

PR 标题：[v11/P0] frontend deprecation + nav collapse + pricing 2-tier
PR 描述贴出：
  - 顶栏视觉截图（关键，PM 用来对齐 RFC §3.2）
  - Pricing 截图
  - 删除文件清单
  - i18n 替换前后 grep 输出
```

---

## 3. P1 — 前端无任务

P1 全部是后端工作（GPT 落 LedgerEntry schema + API + 回填）。前端等 P2 启动。

可选：本阶段你可以**预读 §4 P2 的 prompt**，提前熟悉数据契约。

---

## 4. P2 — AigcStampToggle + LedgerStampBadge + Mock Fixture

### 4.1 给 GLM 的完整 prompt

```text
任务 ID：[v11/P2][FE] AigcStampToggle 组件 + LedgerStampBadge 组件 + ledger/aigc fixture

目标：
  做出 v11 视觉新增的两个核心组件，并预埋 mock fixture，让后续 P3/P4 直接用。
  这一阶段不接真实后端，全部走 fixture。

数据契约（与 GPT 同源，不要改）：

  // LedgerEntry（来自后端 P1 契约）
  type LedgerEntry = {
    id: string;
    workspaceId: string;
    actorType: 'user' | 'agent';
    actorId: string;
    actionKind: string;             // 'workspace.artifact.upload' / 'snapshot.create' / ...
    targetType: string | null;
    targetId: string | null;
    payloadHash: string;            // 64 hex
    prevHash: string | null;
    signature: string;
    signedAt: string;               // ISO
    anchorChain: 'vibehub' | 'zhixin' | 'baoquan';
    anchorTxId: string | null;
    anchorVerifiedAt: string | null;
  };

  // AigcStamp
  type AigcStamp = {
    id: string;
    artifactId: string;
    mode: 'text' | 'image' | 'audio' | 'video';
    explicitMark: string;
    implicitMetadata: Record<string, unknown>;
    providerCode: string;
    contentSerial: string;
    providerApi: 'tencent' | 'aliyun' | 'local';
    stampedAt: string;
    retainedUntil: string;
  };

  // Compliance settings
  type ComplianceSettings = {
    aigcAutoStamp: boolean;
    aigcProvider: 'tencent' | 'aliyun' | 'local';
    ledgerEnabled: boolean;
  };

执行步骤（先 plan）：

  1. 创建 fixture 文件 web/src/lib/data/mock-ledger.ts
     - 导出 mockLedgerEntries: LedgerEntry[]，至少 50 条
     - 覆盖 actionKind：
         workspace.artifact.upload
         snapshot.create
         deliverable.approve
         agent.task.complete
         aigc.stamp.apply
         ledger.anchor.apply
     - 至少 5 条 anchorChain='zhixin'，5 条 'baoquan'，其余 'vibehub'
     - 时间分布在最近 30 天

  2. 创建 fixture 文件 web/src/lib/data/mock-aigc-stamps.ts
     - 导出 mockAigcStamps: AigcStamp[]，至少 20 条
     - 覆盖 4 种 mode 与 3 个 providerApi

  3. 在 web/src/lib/data/mock-persist.ts 已有的 mock 路由器里，
     拦截以下 endpoint 返回 fixture：
       GET  /api/v1/me/ledger
       GET  /api/v1/workspaces/:id/ledger
       GET  /api/v1/workspaces/:id/ledger/export?format=json
       GET  /api/v1/ledger/:id/verify        → { ok: true, brokenAt: null, totalChecked: 1 }
       POST /api/v1/ledger/verify-bundle     → { ok: true, brokenAt: null, totalChecked: N }
       POST /api/v1/artifacts/:id/aigc-stamp
       GET  /api/v1/me/aigc-compliance/audit-trail?month=
       GET  /api/v1/me/compliance-settings
       PATCH /api/v1/me/compliance-settings

  4. 新组件 web/src/components/ledger-stamp-badge.tsx
     视觉规范：
       - 容器：inline-flex，1px dashed border，radius-sm，padding 2px 6px
       - 字体：Geist Mono，11px，颜色 var(--color-text-tertiary)
       - 内容：印戳 emoji 不要用，仅"#" + 短哈希前 8 位 + "·" + 后 2 位
                例：#a1f3b2c4·d5
       - 已锚定（anchorTxId 非 null 且 anchorVerifiedAt 非 null）：
           border 颜色与字体颜色都换为 var(--color-success)
           前面加一个 Check 16px 图标
       - 校验失败（仅在 verify 失败时由父组件传入 broken=true）：
           换为 var(--color-error)
       - 点击行为：调用 onClick prop，由父级决定是否打开 LegalAnchorCard
     props 类型：
       { entry: Pick<LedgerEntry,'signature'|'anchorChain'|'anchorTxId'|'anchorVerifiedAt'>;
         broken?: boolean;
         onClick?: () => void; }

  5. 新组件 web/src/components/aigc-stamp-toggle.tsx
     视觉规范：
       - 容器：复用现有 SectionCard
       - 顶部：开关 + 标题 "AIGC 自动加标" + 副标 "GB 45438-2025 合规"
       - 中部：覆盖率徽章（mono 数字，绿色当 ≥99%，黄色 80-99%，红色 <80%）
       - 底部：警告条（仅在关闭时显示）："关闭后，新生成内容将不自动添加合规标识，可能违反国家相关规定。"
       - provider 单选（local / tencent / aliyun），默认 local
       - 操作：onChange 调 PATCH /api/v1/me/compliance-settings
     props 类型：
       { settings: ComplianceSettings;
         coveragePct: number;
         onChange: (next: ComplianceSettings) => Promise<void>; }

  6. 把 LedgerStampBadge 接到既有 v10 卡片：
     - web/src/components/workspace-artifacts-panel.tsx
     - web/src/components/workspace-snapshots-panel.tsx
     - web/src/components/workspace-deliverables-panel.tsx
     在每张卡片右上角加一个 LedgerStampBadge（如果该实体有 ledgerEntryId）
     fixture 里 mock 这层关联

  7. 不创建 /studio 改造（那是 P3）；不创建 /ledger 页（那是 P4）

  8. E2E：web/tests/e2e/v11-p2-stamp-badges.spec.ts
     - 访问 /work/personal（v10 路径，P2 还没改）
     - 看到 LedgerStampBadge 在 artifact / snapshot / deliverable 卡片上
     - 看到一些已锚定（绿色），一些未锚定（灰色）

不要做：
  - 不要做 /ledger 页（P4 做）
  - 不要做 LegalAnchorCard 完整内容（P6 做，本阶段 onClick 可以留空）

验收：
  - npm run lint --workspace=web
  - npm run typecheck --workspace=web
  - USE_MOCK_DATA=true npm run dev → 手动检查
  - npm run e2e --workspace=web -- v11-p2-stamp-badges

PR 标题：[v11/P2] LedgerStampBadge + AigcStampToggle + ledger/aigc fixtures
PR 描述贴出：组件截图 + fixture 数量统计
```

---

## 5. P3 — Studio Shell + 6 view 整合

### 5.1 给 GLM 的完整 prompt

```text
任务 ID：[v11/P3][FE] StudioShell + /studio 整合 + 6 view tabs

目标：
  把 v10 的 ConsoleShell（左栏 9 项）瘦身为 StudioShell（左栏 4 项），
  把 /work/personal + /work/agent-tasks + /work/library 的功能整合进 /studio
  的 6 个 view tab。

执行步骤（先 plan）：

  1. 重命名（保留向后兼容）：
     - web/src/components/work-console-shell.tsx → web/src/components/studio-shell.tsx
     - 文件内类名 WorkConsoleShell → StudioShell
     - 旧文件可以暂时保留为重新 export，确保 v10 残留页面不挂
     - 左栏只剩 4 项：
         { icon: Compass, href: "/studio", label: t("nav.studio") }
         { icon: Receipt, href: "/ledger", label: t("nav.ledger") }
         { icon: User,    href: `/u/${currentUserSlug}`, label: t("nav.card") }
         { icon: Settings, href: "/settings", label: t("nav.settings") }
       徽标：本月 ledger 数（取自 P3 后端字段）

  2. 重命名：
     - web/src/components/work-view-tabs.tsx → web/src/components/studio-view-tabs.tsx
     - 类名 WorkViewTabs → StudioViewTabs
     - 增加 view 配置：
         tasks / activity / files / snapshots / agents / works
       默认 view = tasks

  3. 创建新页 web/src/app/studio/page.tsx（覆盖 P0 的占位页）
     - 使用 StudioShell + StudioTopBar + StudioViewTabs
     - 各 view 内容从 web/src/app/work/personal/page.tsx 移植
     - 顶部信息条加两个 metric chip：
         "本月 Ledger 数：348"
         "AIGC 标识覆盖率：100%"
       数据来源：GET /api/v1/me/workspaces/personal 的 monthlyLedgerCount + aigcStampCoveragePct
       fixture：mock 这两个字段为 348 / 100

  4. 创建 /studio?view=works 子视图
     - 内容从 web/src/app/work/library/page.tsx 移植（如果还在）
     - 状态 tab 简化为：草稿 / 已公开（删去其他）
     - 列表显示：项目名 / 状态 / 最近更新 / 协作意向数（可选，老数据兼容）

  5. 创建 /studio?view=tasks
     - 内容从 web/src/app/work/agent-tasks/page.tsx 移植
     - 列表分组：进行中 / 待确认 / 已完成 / 失败
     - 每条 TaskCard 右上角加 LedgerStampBadge（P2 已就绪）

  6. 删除 web/src/app/work/personal/ 整个目录
     在 web/src/middleware.ts 加一条 redirect：
       /work/personal* → /studio
     （middleware 是共享文件，PR 描述提示 PM merge）

  7. StudioTopBar
     - 重命名 web/src/components/work-console-shell 内的 WorkspaceTopBar 为 StudioTopBar
       （或抽出为独立文件）
     - 新增右上角："存储 12.7/20 GB" + "新建" 按钮

  8. AigcStampToggle 接入 settings：
     - web/src/app/settings/page.tsx 新增"合规"分区，挂载 AigcStampToggle
     - 数据从 GET /api/v1/me/compliance-settings（mock）

  9. i18n
     - 新 key：nav.studio / nav.ledger / nav.card / studio.view.tasks / ...
     - zh + en 同步

  10. E2E：web/tests/e2e/v11-p3-studio.spec.ts
      - /studio 默认 view=tasks
      - 6 个 view 切换
      - /work/personal redirect 到 /studio
      - 顶部 metric chip 渲染数字

不要做：
  - 不要做 /ledger 页（P4）
  - 不要做 /u/[slug] 改造（P5）

验收：
  - npm run lint --workspace=web
  - npm run typecheck --workspace=web
  - USE_MOCK_DATA=true npm run dev → 手动 smoke 6 个 view
  - npm run e2e --workspace=web -- v11-p3-studio

PR 标题：[v11/P3] StudioShell + /studio with 6 view tabs
```

---

## 6. P4 — /ledger 页 + LedgerTimeline + 导出对话框

### 6.1 给 GLM 的完整 prompt

```text
任务 ID：[v11/P4][FE] /ledger page + LedgerTimeline + LedgerExportDialog

目标：
  做出 v11 第二件事（"签"）的主页面：操作账本可见、可过滤、可导出。

数据契约：
  GET /api/v1/me/ledger?workspaceId=&from=&to=&actor=&kind=&cursor=&limit=
       响应 { items: LedgerEntry[], cursor: string | null }
  GET /api/v1/workspaces/:id/ledger/export?format=json|pdf|txt&from=&to=
       浏览器触发下载

执行步骤（先 plan）：

  1. 新组件 web/src/components/ledger-timeline.tsx
     视觉规范：
       - 垂直时间线，左侧 1px 虚线轨道
       - 每条 entry：
           顶行：actor avatar 20 + actor 名 + Geist Mono 时间（相对：3 分钟前）
           中行：actionKind 翻译后描述（i18n 表）
                 + targetType:targetId chip（mono）
           底行：LedgerStampBadge（P2 已就绪）
       - 不同 actionKind 用不同图标（Upload / Camera / Check / Bot / Stamp / Anchor）
       - 同一天的 entry 用 sticky 日期分隔条（mono "2026-04-19"）
       - cursor 分页："加载更多"按钮在末尾
     props 类型：
       { entries: LedgerEntry[];
         hasMore: boolean;
         loading: boolean;
         onLoadMore: () => void; }

  2. 新组件 web/src/components/ledger-export-dialog.tsx
     视觉规范：
       - 复用现有 ui/modal.tsx
       - 字段：日期范围（from / to，default 本月）
              格式选择（JSON / PDF / TXT，pill 选项，default PDF）
              workspace 自动选中当前 personal
              "导出"按钮触发下载
       - 触发 GET /api/v1/workspaces/:id/ledger/export?... 用 window.location 跳转
     fixture：mock 模式下点击"导出"显示一段 toast "Mock 模式下不下载文件"

  3. 新页 web/src/app/ledger/page.tsx
     - 使用 StudioShell（P3 已就绪）
     - PageHeader：标题"Ledger" + 副标"AI 操作公证账本"
     - 顶部过滤栏：日期范围 / actor 多选（用户 / Agent / 全部）/ kind 多选
     - 主区：LedgerTimeline
     - 右上："导出" 打开 LedgerExportDialog
     - 空态：EmptyState + 文案"还没有 ledger 记录。去 Studio 做点事吧。"

  4. 把 LedgerTimeline 也用在 /studio?view=activity（P3 已留位置）
     /studio?view=activity 显示当前 personal workspace 最近 50 条 LedgerEntry

  5. fixture：mock-ledger.ts（P2 已造）已经够用

  6. i18n actionKind 翻译表
     新 key：ledger.kind.workspace.artifact.upload = "上传文件"
            ledger.kind.snapshot.create = "创建快照"
            ledger.kind.deliverable.approve = "确认交付"
            ledger.kind.agent.task.complete = "Agent 完成任务"
            ledger.kind.aigc.stamp.apply = "添加 AIGC 标识"
            ledger.kind.ledger.anchor.apply = "锚定到司法链"

  7. E2E：web/tests/e2e/v11-p4-ledger.spec.ts
     - 访问 /ledger
     - 看到至少 30 条 entry
     - 过滤 actor=user → 列表更新
     - 过滤 kind=snapshot.create → 列表更新
     - 点击"导出" → dialog 打开 → 选择 PDF + 时间范围 → 触发请求
     - 空态：mock 切到 0 条 → 看到 EmptyState

不要做：
  - 不要做 LegalAnchorCard 详情（P6）
  - 不要做 admin dashboard（P8）

验收：
  - npm run lint --workspace=web
  - npm run typecheck --workspace=web
  - USE_MOCK_DATA=true npm run dev → /ledger 手动 smoke
  - npm run e2e --workspace=web -- v11-p4-ledger

PR 标题：[v11/P4] /ledger page + LedgerTimeline + export dialog
```

---

## 7. P5 — Trust Card

### 7.1 给 GLM 的完整 prompt

```text
任务 ID：[v11/P5][FE] /u/[slug] Trust Card 重做 + TrustMetricGrid + 公开 PDF

目标：
  做出 v11 第三件事（"晒"）：把当前的简单作者主页升级为 Trust Card 公开页。

数据契约：
  GET /api/v1/u/:slug/trust-card  → {
    user: { slug, name, avatarUrl },
    profile: { companyKind, tagline, openToCollab, lookingFor },
    metrics: {
      ledgerEntryCount, snapshotCount, stampedArtifactCount,
      publicWorkCount, avgResponseHours, registrationDays
    },
    publicWorks: [{ slug, name, coverUrl }]
  }
  GET /api/v1/u/:slug/trust-card.pdf  → 下载 PDF

执行步骤（先 plan）：

  1. 创建 fixture web/src/lib/data/mock-trust-card.ts
     - 至少 3 个 user：
         dev-alice：solo_dev、tagline "前端独立开发者"、metrics 各项有数据
         designer-bob：design、tagline "独立 UI 设计师"
         empty-user：所有 metrics 为 0（用于空态展示）
     - mock-persist.ts 拦截 /api/v1/u/:slug/trust-card

  2. 新组件 web/src/components/trust-metric-grid.tsx
     视觉规范：
       - 6 个指标卡，3x2 grid（移动端 2x3）
       - 每张卡：
           顶部 mono 大数字（48px Geist Mono）
           中部小标题（body-sm）
           底部辅助说明（caption）
       - 6 个指标：
           ledgerEntryCount     "Ledger 条数"        "累计可校验操作"
           snapshotCount        "快照数"             "可回滚交付单元"
           stampedArtifactCount "已加标产物"         "GB 45438-2025 合规"
           publicWorkCount      "公开作品"           "可被发现"
           avgResponseHours     "平均响应时长"        "（小时）"
           registrationDays     "活跃天数"           "在 VibeHub"
       - 每个数字 0 时显示 "—" 而不是 "0"
     props：{ metrics: TrustMetrics; }

  3. 重写 web/src/app/u/[slug]/page.tsx
     - 替换现有创作者主页
     - 区块顺序：
         头部 hero：
           头像 96 + 名字 h1 + companyKind 徽章 + tagline
           openToCollab=true 时显示绿色"开放协作"chip
         TrustMetricGrid
         publicWorks 网格（复用 v10 ProjectCard，简化为只读）
         底部："发起协作意向"按钮 → 现阶段先打开 mailto 或显示 toast，
              真正的合作流不在 v11 范围
         右上角："下载 Trust Card PDF" → /api/v1/u/:slug/trust-card.pdf
     - 元数据 metadata 设置：
         OG title: 名字 + " · VibeHub Trust Card"
         OG description: tagline
         OG image: /api/v1/u/:slug/trust-card-og.png（不必本阶段做，先用默认）

  4. 新页 web/src/app/u/[slug]/print/page.tsx（PDF 打印样式）
     - 仅 print: 样式
     - 一页 A4，内容布局：头像 + 名 + tagline + 6 指标 + 二维码（指向 /u/:slug）
     - 用纯 CSS 处理，不引入 PDF 库（PDF 由后端 GPT 生成）

  5. /settings/profile 扩展（共享文件 web/src/app/settings/profile/page.tsx）
     - 增加 OPC Profile 编辑表单：
         companyKind 单选 / tagline 文本 / openToCollab toggle / lookingFor 多选 chip
     - PATCH /api/v1/me/opc-profile（mock 模式下保存到 mock-trust-card.ts）

  6. i18n：
     新 key：profile.companyKind.solo_dev = "独立开发者"
            profile.companyKind.design / content / ops / domain / other
            trust.metrics.ledger / snapshot / stamp / works / response / days

  7. E2E：web/tests/e2e/v11-p5-trust-card.spec.ts
     - 访问 /u/dev-alice：6 指标渲染、有数据
     - 访问 /u/empty-user：6 指标全部显示 "—"
     - 未登录访问 /u/dev-alice：仍可见公开内容
     - 点击 "下载 PDF" 触发 /api/v1/u/:slug/trust-card.pdf 请求
     - /settings/profile 编辑 tagline → 保存 → 回到 Trust Card 看到更新

不要做：
  - 不要做后端 PDF 生成（GPT 负责）
  - 不要做撮合 / 配对 / 私信功能（永久冻结）

验收：
  - npm run lint --workspace=web
  - npm run typecheck --workspace=web
  - USE_MOCK_DATA=true npm run dev → /u/dev-alice 手动 smoke
  - npm run e2e --workspace=web -- v11-p5-trust-card

PR 标题：[v11/P5] /u/[slug] Trust Card + TrustMetricGrid + print page
```

---

## 8. P6 — LegalAnchorCard + 锚定 + Pro 升级提示

### 8.1 给 GLM 的完整 prompt

```text
任务 ID：[v11/P6][FE] LegalAnchorCard + /ledger 锚定动作 + Pro 升级流

目标：
  把 v11 的 Pro 唯一卖点做出来：把 ledger 锚定到至信链 / 保全网。
  Free 用户点击锚定按钮要看到清晰的升级提示。

数据契约：
  POST /api/v1/workspaces/:id/ledger/anchor
       body: { entryIds: string[], chain: 'zhixin' | 'baoquan' }
       response: { attested: LegalAttestationLink[] }
       free 用户调用：返回 403 错误码 PRO_REQUIRED

  type LegalAttestationLink = {
    id: string;
    ledgerEntryId: string;
    chain: 'zhixin' | 'baoquan';
    txId: string;
    proofUrl: string | null;
    verifiedAt: string;
  };

执行步骤（先 plan）：

  1. fixture：在 mock-ledger.ts 已有锚定 entry 的基础上，
     mock-persist.ts 拦截：
       POST /api/v1/workspaces/:id/ledger/anchor
         - 测试用户 isPro=true 时返回 mock attested
         - isPro=false 时返回 403 + { error: { code: 'PRO_REQUIRED', message: '...' } }
     增加 mock 用户配置：USE_MOCK_DATA + ?mock_pro=1 时 isPro=true，否则 false

  2. 新组件 web/src/components/legal-anchor-card.tsx
     视觉规范：
       - 复用 ui/modal.tsx 或 SectionCard
       - 内容：
           "已锚定到 至信链 / 保全网"标题
           Tx ID（mono，可复制）
           verifiedAt 时间
           "外部校验"按钮 → 打开 proofUrl
           "用 vibehub-verify CLI 校验"代码块（mono code 块，含命令示例）
       - 未锚定状态：占位 + "升级 Pro 解锁锚定"按钮

  3. /ledger 页（P4）增强：
     - 列表每条 entry 增加 hover 状态显示"锚定"按钮（仅未锚定的 entry）
     - 顶部增加批量动作工具条（出现在选中 ≥1 条时）：
         "锚定 N 条" + chain 选择 (zhixin / baoquan)
       触发 POST anchor
     - LedgerStampBadge 点击 → 打开 LegalAnchorCard

  4. UpgradePrompt 接入（web/src/components/upgrade-prompt.tsx 已存在）：
     - 当锚定调用返回 403 PRO_REQUIRED 时，显示 UpgradePrompt
     - 文案："锚定到司法链是 Pro 功能。开通后每月可锚定无限条 ledger，
            为你的 AI 工作背书。"
     - CTA：跳 /pricing 或 /settings/subscription

  5. settings/subscription 页（已存在）增加 v11 卖点说明：
     - 顶部加"为什么升级 Pro"小节：
         · 锚定到至信链 / 保全网（司法效力）
         · 月度合规报告 PDF
         · Trust Card 高级版（自定义域名 + 历史合作）
         · 10 GB 存储

  6. i18n：
     legal_anchor.title.zhixin = "至信链"
     legal_anchor.title.baoquan = "保全网"
     legal_anchor.verify_cli = "用 vibehub-verify 命令本地校验"
     pro_required.title = "锚定到司法链是 Pro 功能"
     pro_required.cta = "升级 Pro"

  7. E2E：web/tests/e2e/v11-p6-anchor.spec.ts
     - /ledger 选中 1 条未锚定 entry → 点"锚定" → free 用户看到 UpgradePrompt
     - mock_pro=1 切到 Pro → 锚定成功 → entry 变绿
     - 点击 LedgerStampBadge → 打开 LegalAnchorCard → 看到 Tx ID 和 CLI 命令

不要做：
  - 不要做后端锚定 API（GPT 负责）
  - 不要做 vibehub-verify CLI（GPT 负责）
  - 不要在前端硬编码任何 chain provider 私钥

验收：
  - npm run lint --workspace=web
  - npm run typecheck --workspace=web
  - USE_MOCK_DATA=true npm run dev → 手动 smoke free + Pro 两条路径
  - npm run e2e --workspace=web -- v11-p6-anchor

PR 标题：[v11/P6] LegalAnchorCard + ledger anchor action + Pro gate
```

---

## 9. P7 — 品牌叙事大改

### 9.1 给 GLM 的完整 prompt

```text
任务 ID：[v11/P7][FE] Home 重写 + Pricing 文案大改 + Footer + 顶栏 + 印戳全站

目标：
  把 v10 的"协作"叙事彻底替换为 v11 的"留痕本"叙事。
  这是 v11 用户感知 90% 的来源。

执行步骤（先 plan）：

  1. 重写 Home：web/src/app/page.tsx
     彻底删除 v10 的：HomeFeedSection、HomePillarsClient、HomeCtaFooterClient
     保留：HomeHeroClient（重写文案）

     新区块顺序（top → bottom，每段间距 80px）：

     A. Hero
        - 60/40 布局
        - 左：
            eyebrow（mono caption）"中国 OPC · AI 留痕本"
            H1（display 48）"你和 AI 一起做的工作，"
                            "有据可查。"
            body-lg 描述（≤ 180 字）：
              "VibeHub 自动记录你和 Agent 的每一次写入，加合规标识、留可校验账本、
              沉淀为可外发的信用名片。给客户、给监管、给自己一份证据。"
            两个 CTA 按钮：
              [开始留痕]   apple-accent → /signup
              [查看示例 Card] → /u/dev-alice
        - 右：
            一张静态 SVG 图，画的是"Studio → Ledger → Card"三步工作流
            不要动画，不要 3D，不要光晕

     B. 三件事 section（3 列）
        Studio（做）/ Ledger（签）/ Card（晒）
        每列：图标 40 + 标题 h3 + 描述 body-sm + 跳转链接
              "进入 Studio →" / "查看示例 Ledger →" / "查看示例 Card →"

     C. 监管恐惧 section（强力但克制）
        标题"自 2025-09-01 起，所有 AI 内容必须加合规标识"
        body：引述 GB 45438-2025 罚款上限 1000 万
        4 个小卡片：
          自动加标 / 留痕账本 / 月度合规报告 / 可向监管出示
        参考 stripe.com 的"Compliance"页风格，不要用红色警告色

     D. Pricing 简表（不重复完整 Pricing 页）
        2 卡：Free / Pro，含核心 3 行特性
        底部"查看完整定价 →" 跳 /pricing

     E. 真实 Trust Card 示例
        放 1 张 dev-alice 的 Trust Card 缩略图
        说明"每个 VibeHub 用户都有这样一张可校验的信用名片"

     F. Footer（与 §3 同步）

     原则：
        - 全静态，不调任何 API（fixture 也不调）
        - 不使用任何动效库新组件
        - 中英两版 i18n 同步

  2. 重写 Pricing：web/src/app/pricing/page.tsx
     在 P0 已经简化为两档基础上，加文案：
       Free 卡：
         "1 GB Workspace · 100 ledger/月"
         "基础 AIGC 标识（local）"
         "公开 Trust Card"
         "vibehub-verify CLI 校验"
         "适合：刚开始的独立开发者"
       Pro 卡：
         "10 GB Workspace · 无限 ledger"
         "完整 AIGC 标识（腾讯/阿里 API）"
         "锚定到至信链 / 保全网"
         "月度合规报告 PDF"
         "Trust Card 高级版（自定义域名 + 历史合作）"
         "适合：接外包 / 申请扶持 / 投资尽调的 OPC"
     底部加 FAQ 区：
       Q：为什么不做团队套餐？
       A：v11.0 专注独立开发者。团队功能未来可能作为 v12 单线扩展，
          但不会偏离主线。
       Q：vibehub-verify 是什么？
       A：一个独立 npm 包，本地命令行校验你的 Ledger 是否被篡改。
          不依赖 VibeHub 服务器。
       Q：我的数据所有权？
       A：你的所有 Ledger / Artifact / Snapshot 都可一键导出。

  3. 重写 Footer：web/src/components/footer.tsx
     4 列：
       产品：Home / Studio / Ledger / Card / Pricing
       开发者：API 文档 / vibehub-verify CLI / MCP server
       合规：AIGC 公示 / 隐私 / 条款 / 平台规则
       关于：博客 / 联系
     底部：版权 + 备案号占位

  4. 顶栏 tagline：site-nav.tsx
     - 在 Logo 旁可选加一行 mono caption "AI 留痕本"
     - 如果空间不够就只在移动端展开菜单顶部显示

  5. 印戳元素全站审计：
     - 搜索所有 v10 里 artifact / snapshot / deliverable / agent.task 卡片
     - 没接入 LedgerStampBadge 的位置全部补上
     - 这是 v11 视觉差异化的核心信号，必须铺开

  6. 旧词全清零：
     运行 grep 确认以下词在 web/src/{app,components,locales}/ 下都为 0：
       "协作网络"
       "协作中枢"
       "团队工作空间"
       "协作意向"（除非在归档页面）
       "Workspace 协作"
     仍出现的位置必须改为 v11 词汇或删除整段。

  7. E2E：web/tests/e2e/v11-p7-brand.spec.ts
     - / 顶部 H1 含"有据可查"
     - / 不再含"协作网络"
     - /pricing 仅 2 张卡
     - footer 链接结构正确

不要做：
  - 不要引入新动效库
  - 不要改 globals.css token

验收：
  - npm run lint --workspace=web
  - npm run typecheck --workspace=web
  - USE_MOCK_DATA=true npm run dev → 手动 smoke 全站
  - grep 验证旧词为 0
  - npm run e2e --workspace=web -- v11-p7-brand

PR 标题：[v11/P7] brand narrative rewrite + stamp coverage + footer + faq
```

---

## 10. P8 — PMF 埋点接入 + admin dashboard UI

### 10.1 给 GLM 的完整 prompt

```text
任务 ID：[v11/P8][FE] PMF 三北极星埋点接入 + /admin/v11-pmf-dashboard 页

目标：
  让 RFC §11 的三个北极星指标真实可见。

数据契约（与 GPT 同源）：

  POST /api/v1/internal/pmf/event
    body: { kind: 'compliance.enabled' | 'ledger.exported' | 'subscription.upgraded',
            userId: string,
            payload?: Record<string, unknown> }

  GET /api/v1/admin/v11-pmf-dashboard
    response: {
      complianceEnabledRate:    { value: number, target: 0.7,  trend7d: number[], trend30d: number[] },
      monthlyLedgerExportRate:  { value: number, target: 0.3,  trend7d: number[], trend30d: number[] },
      proConversionRate:        { value: number, target: 0.05, trend7d: number[], trend30d: number[] },
      asOf: string
    }

执行步骤（先 plan）：

  1. 全站埋点接入（仅在 prod 模式才真实上报；dev 与 mock 走 console.log）：
     - AigcStampToggle 切到 on 时：emit('compliance.enabled')
     - LedgerExportDialog 触发下载成功时：emit('ledger.exported', { format })
     - 订阅升级回调（settings/subscription / checkout sandbox 完成回调）：
       emit('subscription.upgraded', { plan: 'pro' })

     抽 helper：web/src/lib/pmf-event.ts
       export function emitPmfEvent(kind, payload?) {
         if (process.env.NEXT_PUBLIC_USE_MOCK_DATA === 'true') { console.log('[pmf]', kind, payload); return; }
         fetch('/api/v1/internal/pmf/event', { method: 'POST', body: JSON.stringify({ kind, payload }) }).catch(noop);
       }

  2. fixture：mock-persist.ts 拦截 GET /admin/v11-pmf-dashboard
     返回示例数据：
       compliance: 0.72 / 0.7
       ledger:    0.28 / 0.3
       pro:       0.04 / 0.05
       trend7d / trend30d 用稍微上升的 sin 波

  3. 新页 web/src/app/admin/v11-pmf-dashboard/page.tsx
     - 仅 admin 角色可见（沿用 v10 admin layout）
     - 三个大卡片：
         每张卡：
           标题（compliance / ledger / pro）
           当前值（mono 大数字 + 百分号）
           target（小字）
           是否达标（绿/黄/红 dot：≥target 绿，≥50%target 黄，<50%target 红）
           7 天 sparkline
           30 天 sparkline
     - 底部："最近一次刷新：asOf"

  4. 接入 admin nav（web/src/app/admin/layout.tsx）
     - 在 admin 侧栏增加："V11 PMF" → /admin/v11-pmf-dashboard

  5. E2E：web/tests/e2e/v11-p8-pmf.spec.ts
     - 以 admin 登录访问 /admin/v11-pmf-dashboard
     - 看到 3 张卡 + sparkline
     - 切 AigcStampToggle → emitPmfEvent 被调用（可用 mock spy）

不要做：
  - 不要做埋点聚合（GPT 负责）
  - 不要给非 admin 用户暴露 dashboard

验收：
  - npm run lint --workspace=web
  - npm run typecheck --workspace=web
  - USE_MOCK_DATA=true npm run dev → /admin/v11-pmf-dashboard 手动 smoke
  - npm run e2e --workspace=web -- v11-p8-pmf

PR 标题：[v11/P8] pmf event hooks + admin dashboard UI
```

---

## 11. 全阶段共同的提交红线

每个 PR 在合并前 PM 会检查：

1. 只动了"你负责区域"的文件，没碰后端
2. 提交前本地测试输出贴在 PR 描述里
3. 所有 API 调用都有 mock fixture，能在 `USE_MOCK_DATA=true` 下跑通
4. 共享文件改动有 `@PM 需手动 merge` 提示
5. 没有引入新依赖，除非 PR 描述里说明并由 PM 同意
6. commit message 用 `[v11/PN]` 开头
7. 不修改 `docs/v11.0-*` 任何文档（除非 PM 让你改）
8. 视觉新增元素只允许"印戳"一个，不要顺手加其他视觉花样

---

## 12. 启动方式（PM 操作）

1. 通读 `docs/v11.0-final-chapter-rfc.md` 与本文件
2. 把本文件 §0 完整复制给 GLM 作为系统消息
3. 然后把 §2.1（P0 prompt）作为第一条用户消息发给 GLM
4. 等 GLM 回 plan → 你 review → 同意后让它执行
5. PR 来后 review，本地手动 smoke + 跑 E2E
6. 通过则进入下一阶段（P1 全交给 GPT，前端从 P2 开始）
7. 严格按 P0 → P2 → P3 → ... → P8 顺序，**禁止跳阶段**
